import { Content } from "@/components/ui/Content";
import { Header } from "@/components/ui/Header";

export default function AudioNotesDetailsScreen({ id }: { id: string }) {
  return (
    <Content>
      <Header title="Detalles audio" />
    </Content>
  );
}
